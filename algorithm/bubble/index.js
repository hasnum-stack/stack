const { swap } = require("../utils/index");
console.log(swap);
const array = [5, 3, 8, 6, 4];

function bubbleSort1(array) {
  //确定冒泡的次数
  for (let i = 1; i < array.length; i++) {
    console.log(i);
    // for()
  }
}
bubbleSort1(array);
