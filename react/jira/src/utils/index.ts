// const requestCleaner = value => (value === 0 || value === false ? false : !value);
// const cleanObject = (object: Object) => {
//   const result = { ...object };
//   Object.keys(result).forEach(key => {
//     const value = result[key];
//     if (requestCleaner(value)) {
//       delete result[key];
//     }
//   });
//   return result;
// };
// export { cleanObject };
